#!/usr/bin/env sh

##############################################################################
##
##  Gradle start up script for POSIX generated by Gradle.
##
##############################################################################

set -e

APP_NAME="Gradle"
APP_BASE_NAME=$(basename "$0")

DEFAULT_JVM_OPTS='"-Xmx64m" "-Xms64m"'

MAX_FD="maximum"

warn () {
    echo "$*"
}

die () {
    echo
    echo "$*"
    echo
    exit 1
}

cygwin=false
msys=false
darwin=false
nonstop=false
case "$(uname)" in
  CYGWIN* ) cygwin=true ;;
  Darwin* ) darwin=true ;;
  MSYS* | MINGW* ) msys=true ;;
  NONSTOP* ) nonstop=true ;;
esac

CLASSPATH=$APP_HOME/gradle/wrapper/gradle-wrapper.jar

APP_HOME=$(cd "$(dirname "$0")" && pwd)

if [ ! -r "$CLASSPATH" ]; then
  die "Missing $CLASSPATH.
This repository includes wrapper scripts and properties, but not the wrapper JAR.
On another machine with Gradle installed, run: gradle wrapper
Or open this folder in Android Studio which will generate the wrapper files."
fi

if [ "$cygwin" = "true" -o "$msys" = "true" ] ; then
    APP_HOME=$(cygpath --path --mixed "$APP_HOME")
    CLASSPATH=$(cygpath --path --mixed "$CLASSPATH")
fi

JAVA_HOME=${JAVA_HOME:-}
if [ -z "$JAVA_HOME" ] ; then
    JAVA_EXE=$(command -v java || true)
    [ -n "$JAVA_EXE" ] || die "JAVA_HOME is not set and no 'java' command could be found."
else
    JAVA_EXE="$JAVA_HOME/bin/java"
fi

exec "$JAVA_EXE" $DEFAULT_JVM_OPTS $JAVA_OPTS $GRADLE_OPTS \
  -classpath "$CLASSPATH" org.gradle.wrapper.GradleWrapperMain "$@"

